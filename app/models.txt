resource = false
Addresses string( address_1 address_2 city state ) integer( addressable_id zip )
Bonuses integer( user_id ) boolean( eight_in_eight twelve_in_twelve )
Cart integer( product_id )
Commissions integer( user_id amount ) text( description )
Content integer( page_id ) string( section ) text( content )
Email_messages string( subject ) text( body )
Email_recipients integer( sms_message_id user_id )
Images string( type url )
Levels integer( user_id ancestor_id level )
Mobile_plans string( name ) text( blurb description )
Products string( name ) text( blurb description ) integer( price quantity )
Profiles string( public_name ) text( public_content ) boolean( receive_company_email receive_company_sms receive_upline_email receive_upline_sms receive_downline_email receive_downline_sms)
Ranks string( name )
Roles string( name )
Reviews integer( product_id rating ) text( comment )
Pages string( name url type ) boolean( opportunity )
Payments integer( user_id transaction_id amount ) string( tender ) text( details )
Sales integer( product_id, user_id, sponsor_id, quantity )
Sms_messages string( body )
Sms_recipients integer( email_message_id user_id )
User_products integer( product_id )
User_ranks integer( user_id rank_id )
Users string( first_name last_name email password key code ) integer( phone role_id sponsor_id mobile_plan_id min_commission )