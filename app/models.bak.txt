resource = false
Address string( address_1 address_2 city state ) integer( addressable_id zip ) boolean( disabled )
Bonus integer( user_id ) boolean( eight_in_eight twelve_in_twelve disabled ) 
Cart integer( product_id ) boolean( disabled )
Email_message integer( sender_id recipient_id ) text( subject body ) boolean( disabled )
Image string( type url disabled )
Level integer( user_id ancestor_id level ) boolean( disabled )
Opportunity string( title ) text( body ) boolean( include_form public customers reps disabled ) integer( deadline )
Page string( title url type ) text( body ) boolean( disabled )
Product string( name ) text( blurb description ) integer( price quantity category_id ) boolean( disabled )
Product_category string( name ) boolean( disabled )
Profile string( public_name ) text( public_content ) boolean( receive_company_email receive_company_sms receive_upline_email receive_upline_sms receive_downline_email receive_downline_sms disabled )
Rank string( name ) boolean( disabled )
User_site integer( user_id ) text( body )
Review integer( product_id rating ) text( comment ) boolean( disabled )
Role string( name ) boolean( disabled )
Sale integer( product_id, user_id, sponsor_id, quantity ) boolean( disabled )
Sms_message integer( sender_id recipient_id ) text( body ) boolean( disabled )
User_site integer( user_id ) text( title body ) string( banner ) boolean( display_phone )
User string( public_id first_name last_name email password gender key ) date( dob ) integer( phone role_id sponsor_id mobile_plan_id min_commission ) boolean( disabled )
User_product integer( product_id ) boolean( disabled )
User_rank integer( user_id rank_id ) boolean( disabled )
Uvent string( name ) text( description ) integer( date_start date_end ) boolean( public customers reps editors admins disabled )


Commission integer( user_id amount ) text( description ) boolean( disabled )
Payment integer( user_id transaction_id amount ) string( tender ) text( details ) boolean( disabled )
